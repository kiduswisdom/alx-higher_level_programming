#!/usr/bin/node

const myArray = ['C lang is fun', 'Python is cool lang', 'Javascript is amazing'];

for (let index = 0; index < myArray.length; index++) {
  const element = myArray[index];
  console.log(element);
}
